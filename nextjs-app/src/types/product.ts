export type Product = {
  id: string
  name: string
  slug: string
  category: string
  shortDescription: string
  description: string
  images: string[]
  applications: string[]
  finishes: string[]
  active: boolean
}
